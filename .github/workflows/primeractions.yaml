name: miprimieracion

on:
  push:
    branches:
      - feature-**
    branches-ignore:
      - feature-unai-**

jobs:
    lamine:
        runs-on: ubuntu-latest
        steps:
            - name: Primera accion
              run: echo "Hola mundo"