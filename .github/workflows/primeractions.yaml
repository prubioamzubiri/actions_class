name: miprimieracion

on:
  push:
    branches:
      - main

jobs:
    lamine:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v5.0.0
            - name: LS
              run: ls -l
